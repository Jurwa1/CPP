#include <iostream>
#include <cctype>

int main() {
	std::string Password;
	std::cout << "Enter password. A good password should contain special characters, lowercase and uppercase letters, and be longer than 7 characters.\n";
	while (true) {
		std::cin >> Password;
		for (int i{ 0 }, j{ Password.length() }; i < j; ++i) {

		}
	}
}